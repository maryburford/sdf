
This is "#{@user.user_name}"
= link_to "Message #{@user.user_name}", new_message_path({receiver_id: @user.id})
= link_to "#{@user.user_name}'s Answers", user_answers_show_path({id: @user.profile.id})
- if defined? @profile.profile_photo
	= image_tag "#{@profile.profile_photo}"
- else
	fuck